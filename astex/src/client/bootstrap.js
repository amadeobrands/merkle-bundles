// Webpack bootstrap code.
// const pjson = require('../package.json');
// const { name, version } = pjson;
// import codecache from './codecache';

// export default function bootstrap() {
//     codecache.get(`${name}@${version}`)
//     .then(code => {
//         if(!code) {
//             // get the code from source.
//         }

//         let mod = eval(code);
//         return mod;
//     })
// }

/*
// eval current code
eval(code, moduleids)
.then(() => {
    webpackBootstrap(modules)
})
*/